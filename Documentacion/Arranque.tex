%!TEX root =  tfg.tex
\chapter{Arranque}

\begin{quotation}[Empresario y cineasta]{Walt Disney (1901--1966)}
The best way to get started is to quit talking and begin doing.
\end{quotation}

\begin{abstract}
Este capítulo define la lista inicial de características priorizadas del proyecto Scubex y, a partir de ellas, la arquitectura base necesaria para soportarlas. Se sigue el orden natural del ciclo FDD: primero se establece qué se va a construir, y solo entonces se diseña cómo.
\end{abstract}

\section{Lista de características}

Siguiendo la fase 1 de FDD (Desarrollar lista de características) y los objetivos definidos para el proyecto, se han identificado las siguientes funcionalidades nucleares priorizadas por valor para el usuario:

\begin{enumerate}
    \item \textbf{Gestión de Usuarios (OAuth2)}: Autenticación delegada mediante Google y persistencia de perfil (nombre, foto). El usuario puede personalizar su nombre y foto de perfil desde la página de perfil, con soporte para eliminar la cuenta.
    \item \textbf{Mapa Interactivo}: Visualización de un mapa navegable mediante MapLibre GL JS como superficie central de interacción, con búsqueda de lugares integrada (Nominatim).
    \item \textbf{Escáner de Especies}: Identificación de fauna marina en una zona mediante radio de búsqueda y validación cruzada de APIs científicas (OBIS + iNaturalist), con sistema de caché adaptativo (TTL 48h) para reducir latencia y llamadas externas.
    \item \textbf{Datos Climáticos y Oceanográficos}: Condiciones de la zona bajo demanda mediante Open-Meteo (viento, temperatura del agua, corrientes, olas, datos atmosféricos), con evaluación de aptitud para el buceo y caché de 30 minutos en el backend.
    \item \textbf{Registro de Publicaciones}: Publicación de inmersiones en el mapa con título, descripción, fotografía y geolocalización inversa (nombre del lugar mediante Nominatim). Edición y borrado por el propio autor. Cada publicación genera un enlace compartible (\texttt{/map?pub=\{id\}}).
    \item \textbf{Interacciones Sociales}: Sistema de likes, guardados y comentarios sobre publicaciones. Soporte de menciones en comentarios mediante la sintaxis \texttt{@[Nombre](email)} con notificaciones automáticas al mencionado. El enlace compartible de cada publicación es accesible mediante un botón que usa la Web Share API en móvil y copia al portapapeles en escritorio.
    \item \textbf{Red Social y Seguimiento de Usuarios}: Perfiles públicos por usuario con recuento de seguidores y seguidos, posibilidad de seguir/dejar de seguir, y vista de publicaciones de cada perfil. Página de perfil propio con mis publicaciones y publicaciones guardadas. Cada perfil dispone de enlace compartible (\texttt{/user/\{email\}}) accesible desde un botón de compartir en la página de perfil.
    \item \textbf{Sistema de Notificaciones}: Notificaciones en tiempo real (campana en el header) para eventos de seguimiento, like, comentario y mención. Las notificaciones se marcan como leídas al abrirlas o se eliminan permanentemente con swipe (móvil) o botón (escritorio).
\end{enumerate}

\textbf{Justificación de priorización}: La lista anterior refleja el orden de implementación planificado, de mayor a menor prioridad. A continuación se justifica ese orden:
\begin{itemize}
    \item La autenticación se implementa primero ya que el resto de funcionalidades (avistamientos, perfil, red social) dependen de la identidad del usuario.
    \item El mapa es la superficie central de la aplicación: sin él no tiene sentido geolocalizar especies, condiciones climáticas ni avistamientos.
    \item El escáner de especies, los datos climáticos y el registro de publicaciones constituyen el MVP funcional mínimo. Como se planteó en el capítulo de Contexto, el objetivo es que el usuario pueda tomar decisiones apoyándose tanto en datos objetivos como en experiencias reales: el escáner y el clima aportan la primera dimensión; las publicaciones, la segunda, al permitir consultar las inmersiones documentadas por otros buceadores en la misma zona.
    \item Las funcionalidades de comunidad (likes, comentarios, seguimiento entre usuarios, notificaciones) se implementan sobre ese MVP: convierten la herramienta en una red social de buceadores, pero no son necesarias para que la propuesta de valor principal sea utilizable.
\end{itemize}

\section{Diseño arquitectónico}
\label{sec:arquitectura}

Con las características identificadas, el sistema requiere una arquitectura capaz de orquestar múltiples APIs externas, gestionar identidad de usuario y servir una interfaz cartográfica interactiva. Se ha optado por una \textbf{arquitectura cliente-servidor desacoplada} con patrón Gateway de APIs.

\subsection{Stack Tecnológico}

\begin{itemize}
    \item \textbf{Backend}: Spring Boot 4 (Java 21). Actúa como Gateway de APIs científicas, orquestador de peticiones asíncronas y gestor de autenticación.
    \item \textbf{Frontend}: React 18 + Vite. Con Hot Module Replacement (HMR) para desarrollo ágil. Gestión de estado reactiva mediante MobX \texttt{makeAutoObservable}, con stores separadas por dominio (especies, clima, publicaciones, usuario, mapa).
    \item \textbf{Cartografía}: MapLibre GL JS. Motor de renderizado vectorial para animaciones fluidas y bajo consumo de datos.
    \item \textbf{Base de Datos}: H2 en memoria para el entorno de desarrollo y tests unitarios. PostgreSQL 16 para preproducción y producción, gestionado como servicio en Railway.
    \item \textbf{Testing}: JUnit 5 + Mockito para pruebas unitarias del backend. Jacoco para generar reportes. H2 en memoria como base de datos de test.
    \item \textbf{Seguridad}: Spring Security con OAuth2 Client para Google Login.
    \item \textbf{Documentación de API}: OpenAPI 3.0 (Swagger UI) con anotaciones automáticas.
    \item \textbf{Despliegue}: Railway (backend + PostgreSQL) y Vercel (frontend).
\end{itemize}

\subsection{Estrategia de entornos}

Se han definido tres entornos que reflejan el ciclo de vida del software desde el desarrollo local hasta la puesta en producción:

\begin{table*}[htb]
    \centering
    \begin{coolTable}{p{3cm}p{3cm}p{3cm}p{\textwidth-9.5cm}}{4}
{Estrategia de entornos de Scubex}
\textbf{Entorno}&\textbf{Infraestructura}&\textbf{Base de datos}&\textbf{Rama Git}\\
\midrule
Desarrollo&Local&H2 en memoria&Cualquiera\\
Preproducción&Railway + Vercel&PostgreSQL (Railway)&\texttt{develop}\\
Producción&Railway + Vercel&PostgreSQL (Railway)&\texttt{main}\\
    \end{coolTable}
    \caption{Estrategia de entornos del proyecto}
\end{table*}

\textbf{Justificación de la separación entre plataformas}:
\begin{itemize}
    \item \textbf{Vercel} gestiona el frontend React/Vite: Se despliega automáticamente en cada \texttt{push}. Se cuenta con previews por rama sin configuración adicional, más que establecer las direcciones de backend de producción y preproducción.
    \item \textbf{Railway} gestiona el backend Spring Boot y PostgreSQL en el mismo proyecto. Permite tener varios entornos distintos de una misma aplicación desplegados simultaneamente, lo que la hace idonéa para combinarla con Vercel.
    \item La separación permite desplegar frontend y backend de forma independiente. Gracias a esto pueden escalarse independientemente y no solo eso. Debido a la separación se cuenta con mas memoria de los planes gratuitos de ambas aplicaciones, reduciendo costes. 
\end{itemize}

La configuración CORS se ha establecido manualmente mediante la variable \texttt{CORS\_ALLOWED\_ORIGINS} en Railway, y \texttt{VITE\_API\_BASE\_URL} en Vercel, configuradas con las respectivas URL del backend correspondientes a preproducción y producción.

\subsection{Justificación de MapLibre GL JS + MapTiler}

La solución cartográfica se compone de dos piezas complementarias:

\begin{itemize}
    \item \textbf{MapLibre GL JS} actúa como motor de renderizado: biblioteca open source (fork de Mapbox GL JS) que renderiza estilos vectoriales mediante WebGL. Permite animaciones fluidas, rotación e inclinación del mapa, y menor consumo de datos frente a tiles rasterizados. Al ser open source, no impone restricciones de licencia sobre el código.
    \item \textbf{MapTiler} provee los tiles y el estilo visual del mapa a través de su API (\texttt{VITE\_MAPTILER\_KEY}). Se ha creado un estilo personalizado (tema marino) alojado en MapTiler Cloud. El plan gratuito cubre de sobra el volumen de peticiones necesario para el proyecto.
\end{itemize}

La separación entre motor de renderizado (MapLibre) y proveedor de tiles (MapTiler) permite sustituir el proveedor de tiles sin cambiar el código de renderizado, y viceversa.

\subsection{Integración de APIs Externas}

\begin{table*}[htb]
	\centering
	\begin{coolTable}{p{3.5cm}p{3.5cm}p{\textwidth-7.5cm}}{3}
{APIs externas integradas en Scubex}
\textbf{API}&\textbf{Proveedor}&\textbf{Datos proporcionados}\\
\midrule
OBIS&Ocean Biodiversity Information System& Ocurrencias biológicas: nombre científico, coordenadas, taxonomía (phylum), fecha de avistamiento~\cite{obisglobal}.\\
iNaturalist&iNaturalist.org& Metadatos multimedia: fotos de especies, nombres comunes (\texttt{preferred\_common\_name})~\cite{inaturalist}.\\
Open-Meteo&Open-Meteo.com& Datos climáticos y oceanográficos: viento, temperatura del agua/aire, altura de olas, corrientes marinas, precipitaciones. API gratuita sin necesidad de clave y con plan gratuito generoso~\cite{openmeteo}.\\
Nominatim&OpenStreetMap Foundation& Geocodificación directa e inversa. Directa (\texttt{/search}): buscador de lugares del mapa. Inversa (\texttt{/reverse}): obtención del nombre humano de unas coordenadas (playa, municipio, estado) al crear publicaciones y al visualizar su detalle.\\
	\end{coolTable}
	\caption{Resumen de APIs externas}
\end{table*}

\textbf{Estrategia de consumo}:
\begin{itemize}
    \item \textbf{OBIS + iNaturalist}: Consultas de especies marinas mediante el botón de escaneo.
    \item \textbf{Open-Meteo}: Consulta bajo demanda al interactuar con el mapa.
    \item \textbf{Nominatim}: Consulta bajo demanda al interactuar con el mapa. Se usa en el buscador de lugares (\texttt{/search}) y al crear una publicación para obtener el nombre humano de las coordenadas seleccionadas (\texttt{/reverse}).
\end{itemize}

\textbf{Decisiones pendientes que quedaron en esta fase}: Quedó pendiente el determinar la estrategia de caching para OBIS que se resolvió en la Iteración~1; Y también el caching de condiciones climáticas que se resolvió en la Iteración~2. 

\subsection{Diagrama de despliegue}

La Fig.~\ref{fig:umlDespliegue} muestra la topología de despliegue de Scubex: los nodos físicos o lógicos que ejecutan cada componente y los canales de comunicación entre ellos.

\begin{figure}[htbp]
\begin{center}
\includegraphics[width=\textwidth]{figures/umlDespliegue.pdf}
\caption{Diagrama UML de despliegue de Scubex\label{fig:umlDespliegue}}
\end{center}
\end{figure}

El navegador del usuario se comunica con dos nodos de producción: Vercel, que sirve la aplicación React compilada como artefacto estático mediante HTTPS, y el backend en Railway, al que envía las peticiones REST autenticadas con JWT. Las llamadas al backend deben pasar el filtro CORS configurado mediante \texttt{CORS\_ALLOWED\_ORIGINS}.

Los consumos externos se reparten en dos grupos según quién los origina. El frontend llama directamente a MapTiler Cloud para obtener los tiles y el estilo del mapa, y a Nominatim para la búsqueda y geocodificación inversa de lugares. El backend es el que se comunica con las APIs científicas y de datos: OBIS e iNaturalist para el escáner de biodiversidad, Open-Meteo para los datos climáticos y oceanográficos, y Google OAuth2 para validar los tokens de autenticación. El backend accede a PostgreSQL en Railway mediante JDBC.
